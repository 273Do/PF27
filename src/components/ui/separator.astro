---
import { cn } from "@/lib/utils";

export type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<div
  class={cn(
    "pointer-events-none my-4 border-[0.1px] border-dashed border-muted-foreground",
    className,
  )}
>
</div>
